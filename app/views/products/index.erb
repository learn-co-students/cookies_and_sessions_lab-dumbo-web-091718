<h1>Cart!</h1>

<% if @cart != nil %>
  <%= @cart.each do |product| %>
    <%= product %>
  <% end %>
<% end %>

<%= form_tag ({controller: 'products',
    action: 'add',
    method: 'post'}) do %>

  <input name=product>
  <input type=submit value='add to cart'>
<% end %>
